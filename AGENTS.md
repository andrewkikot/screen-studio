# AGENTS.md — Context for AI Agents

Working notes for any AI coding session touching this repo. Read before changing code.

## What this is

**Screenshot Studio** — cross-platform (Win/macOS/Linux) tray-resident screenshot capture tool with a built-in annotation editor. Electron 33 + TypeScript + React 18 + Vite (via `electron-vite`) + Konva.js. No backend; everything is local.

## Commands

```powershell
npm run dev          # dev mode (HMR); auto-generates icons first
npm run typecheck    # MUST pass: runs BOTH tsconfig.node.json and tsconfig.web.json
npm run build        # production build -> out/
npm run dist         # build + electron-builder installers
node scripts/gen-icons.mjs   # regenerates build/icon.png/.ico/.icns, resources/tray.png (dependency-free PNG/ICNS encoder)
```

There is **no lint config**. `npm run typecheck` is the gate — always run it after edits.

## Architecture

Three Electron pages share one preload bridge (`window.api`, typed in `src/renderer/src/env.d.ts`):

| Page | Entry | Purpose |
|---|---|---|
| overlay | `src/renderer/src/overlay/` | transparent fullscreen region-select |
| editor | `src/renderer/src/editor/` | Konva annotation canvas |
| settings | `src/renderer/src/settings/` | hotkey recorder, save folder, format |

All pages are declared as separate inputs in `electron.vite.config.ts` — add new pages there too.

Flow: hotkey/tray → main grabs EVERY display in one `desktopCapturer.getSources` call (sources matched to `screen.getAllDisplays()` via `display_id`, index-fallback) → one overlay/picker window per display, each showing its own frozen frame (shots keyed by `win.id` in `overlayShots`/`pickerShots` maps, resolved per-IPC via `event.sender.getId()`) → selection rect from whichever overlay → cropped in main (`nativeImage.crop`) → editor window opens with PNG dataURL → export to clipboard or file.

Key modules:
- `src/main/index.ts` — windows, tray, globalShortcut, all ipcMain handlers
- `src/main/config.ts` — hand-rolled JSON settings store at `userData/settings.json`
- `src/main/updater.ts` — electron-updater state machine (GitHub Releases feed); disabled when unpackaged
- `src/shared/types.ts` — single source of truth for IPC payloads and shape models; imported by BOTH tsconfig projects (keep it type-only)

## Conventions & gotchas (learned the hard way)

- **Strict TS everywhere**, no `any`. React function components annotated with `ReactElement`.
- **Konva shapes are stored in original-image pixel coordinates**; the stage is scaled by `view = fit-to-window`. Export uses `stage.toDataURL({ pixelRatio: 1 / view })` to restore full resolution. Don't "fix" this ratio.
- Ellipse shapes store bbox x/y (top-left) but render center-based — dragEnd math differs from rect (see `handleDragEnd`).
- Blur tool = cached `Img` node with crop + Pixelate filter; must call `node.cache()` after geometry changes.
- Import filters as `konva/lib/filters/Pixelate` — bare `konva/filters/Pixelate` has no types (package has no exports map).
- `react-konva` exports `Image` (aliased here as `KImage`) and `Rect` (aliased `KRect` because it clashes with the shared `Rect` type).
- **DPI**: captures are physical pixels; UI works in logical. Multiply by `display.scaleFactor` when crossing the boundary. The overlay `<img>` MUST be stretched to `width/height:100%` or HiDPI users see a zoomed screen.
- `electron-store` was deliberately avoided (ESM-only breaks CJS main). Use `config.ts`.
- Settings window re-registers the global hotkey on change and reverts if registration fails (checks `globalShortcut.isRegistered`).
- Tray icon path differs packaged vs dev — always go through `resourcePath()`.
- Updater swaps the tray icon to `tray-update.png` (red dot) when an update is available; both icons are generated by `scripts/gen-icons.mjs`.
- **Linux/Wayland**: on Wayland sessions (`WAYLAND_DISPLAY`/`XDG_SESSION_TYPE=wayland` + `DISPLAY` present) the main process RE-EXECS itself with a real `--ozone-platform=x11` argv flag (guarded by `SS_X11_REEXEC`, parent idles until child exits). The child must ALSO get `--no-sandbox` when `unshare -Ur` fails (Ubuntu 24.04+ AppArmor blocks user namespaces; SUID chrome-sandbox can never work inside an AppImage FUSE mount) — bypassing AppRun loses its automatic `--no-sandbox`, so the re-exec probes `unshare -Ur` itself and appends the flag, with a retry-on-crash fallback. Native-Wayland capture goes through xdg-desktop-portal ScreenCast, which pops GNOME's "select a monitor to share" dialog before every grab and makes globalShortcut unreliable — this was the "app dead until I pick a monitor" bug on Ubuntu. Do NOT use `app.commandLine.appendSwitch('ozone-platform', ...)` for this: Chromium picks the Ozone backend before main JS runs, so in-code switches are too late (Vesktop hit the same). Opt out with `SS_OZONE=wayland`. Do NOT add `--no-sandbox` via appendSwitch in the normal path (see electron-builder bullet below). Startup forensics go to `userData/ss-startup.log` (debugLog helper).
- **Multi-window overlays**: overlays/pickers live in arrays + `Map<windowId, Grab>`. Blur-to-cancel must be debounced (~150ms) and only fire when NO sibling window is focused — focusing overlay B fires blur on overlay A.
- **electron-builder must stay ≥ 26.9**: Ubuntu 24.04+ blocks unprivileged user namespaces (AppArmor), and the setuid `chrome-sandbox` can never work inside an AppImage FUSE mount → Electron aborted with "SUID sandbox helper ... not configured correctly". 26.9+ generates an AppRun that probes `unshare -Ur true` and only then launches with `--no-sandbox`; it also ships an AppArmor profile for the deb target. Do NOT add `--no-sandbox` via `app.commandLine.appendSwitch` — sandbox init happens before main-process JS runs.
- electron-builder 26 needs **Node ≥ 22** locally (`@noble/hashes` v2 is ESM-only; `require(esm)` needs 22.12+). CI already pins Node 22.
- After changing code: `npm run build`, then kill `electron` processes and relaunch `node_modules\electron\dist\electron.exe` with working dir = repo root to test the built app.

## Releasing

Releases are cut by `.github/workflows/release.yml`: push a tag `v*` (matching `package.json` version, which MUST be bumped first) and it builds/publishes installers + `latest.yml`/`.blockmap` to a GitHub Release on all three OSes. electron-updater only sees **published** releases, never drafts.

- Bump version → commit → `git tag v0.1.1 && git push origin main --tags`
- Signing is env-driven; without secrets everything still publishes unsigned:
  - Windows/mac: `CSC_LINK`, `CSC_KEY_PASSWORD` (macOS updates REQUIRE a signed app)
  - macOS notarization: `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `APPLE_TEAM_ID`
- Linux: AppImage auto-updates; the deb target does NOT (electron-updater limitation) — deb ships as-is.
- First real update test: install packaged vN, publish vN+1, watch tray flow (dot → download % → restart installs).

## Known limitations / roadmap ideas

- Multi-monitor: every display gets an overlay/picker window; displays whose thumbnail comes back empty are skipped. Native-Wayland users who set `SS_OZONE=wayland` get the portal monitor-picker dialog back.
- No settings entry yet for capture delay or JPEG quality
- macOS needs Screen Recording permission; Linux needs a compositor supporting transparency
- Editor has no zoom/pan, no resize handles for pen/arrow lines
